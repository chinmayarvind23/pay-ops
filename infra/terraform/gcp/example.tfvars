# Keep false for inspection. Enabling and applying creates billable resources.
enabled       = false
project_id    = "your-payops-project"
region        = "us-central1"
operator_cidr = "192.0.2.1/32" # Documentation address: replace before a real deployment.
